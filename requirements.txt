# Core framework (LTS support window currently extends through 2026)
Django>=4.2,<4.3

# Windows developers need the IANA time zone database since zoneinfo is absent
tzdata>=2024.1; sys_platform == "win32"

# Load environment variables from .env files
python-dotenv>=1.0,<2.0
shapely>=2.0,<3.0
